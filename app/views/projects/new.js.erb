$("#spec-modal").html("<%= j(render 'new') %>")
$('#new_project').validate();
<% unless @project.errors.any? %>
    $("#spec-modal").openModal();
<% end %>

$('.modal-close').on('click', function() {
    $('#spec-modal').closeModal();
});