var tagElem = $("#<%=@spec.id%> span.tags");
$('#spec-modal').closeModal();
tagElem.html("<%= j(render partial: "tags/show", collection: @new_tags_hash, as: :tag) %>");
$('#<%=@spec.id%> .delete_tag').show();
