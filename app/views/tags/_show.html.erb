<div class="chip tag small-chip" id="tag<%=tag[:id]%>" style="background-color: <%= tag[:color] %>">
    <%= tag[:name] %>
    
    <%= link_to tag_path(:id => tag[:id]), 
                method: :delete, 
                :remote => true, 
                :class => 'delete_tag' do %>
        <i class="small-chip-icon material-icons">close</i>
    <% end %>
</div>