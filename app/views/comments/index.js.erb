$("#spec-modal").html("<%= j(render 'modal') %>")
$("#spec-modal").openModal();

$('.comment-btn').on('click', function() {
    var btn = $('.comment-btn').prop('disabled', true);
    $(this.form).submit();
});


