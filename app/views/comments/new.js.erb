$(function(){
    $('.comment-button').popover({ 
        html : true,
        placement : 'left',
        title : 'comments',
        content: function() {
          return "<%= j(render 'comments/form') %>";
        }
    })
});